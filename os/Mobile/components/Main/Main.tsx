import AppGrid from './AppGrid';
import styles from './Main.module.scss';

const Main = () => {
	return (
		<div className={styles.container}>
			<AppGrid />
		</div>
	);
};

export default Main;
