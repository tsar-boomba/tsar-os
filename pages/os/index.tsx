import OS from '@/os';
import Head from 'next/head';

const OSPage = () => {
	return (
		<>
			<Head>
				<title>Tsar OS</title>
			</Head>
			<OS />
		</>
	);
};

export default OSPage;
